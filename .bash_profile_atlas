# .bash_profile

# Get the aliases and functions
if [ -f ~/.bashrc ]; then
	. ~/.bashrc
fi

# load common modules
# module load GLIBC_2.14 gcc-4.8.2

# User specific environment and startup programs
# Homebrew
shopt -s direxpand
eval $($HOME/.linuxbrew/bin/brew shellenv)

# export HOMEBREW_HOME=$HOME
export HOMEBREW_NO_AUTO_UPDATE=1
export HOMEBREW_BUILD_FROM_SOURCE=1
export EDITOR=nvim
# export HOMEBREW_PATH=$HOME/usr/bin:$HOMEBREW_PREFIX/bin:$HOMEBREW_PREFIX/sbin
# export HOMEBREW_ISYSTEM_PATHS=$HOME/usr/include
# export HOMEBREW_CORE_GIT_REMOTE=https://github.com/wangsen992/linuxbrew-core


#eval $(/home/svu/e0305043/.linuxbrew/bin/brew shellenv)
#export HOMEBREW_EDITOR=nvim

# Alias
alias src-of="source ~/hpctmp/usr/opt/openfoam/etc/bashrc"
